{
  "name": "neatify-car-care",
  "version": "1.0.0",
  "description": "Neatify vehicle care storefront with admin dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.2",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1"
  }
}
