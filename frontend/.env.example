# VITE_API_URL is the base URL for the backend API.
# Leave it blank or comment it out if deploying to Vercel (since vercel.json rewrites API calls to the serverless function).
# Only use this if you deploy the frontend and backend separately on different domains.
VITE_API_URL=http://localhost:3000
